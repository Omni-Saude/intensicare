{
  "$description": "Theme resolution map — dark is the default theme. Toggled via <html data-theme=\"dark|light\">, no bundle swap, no reload.",
  "source_ref": "docs/plan/design/design-tokens.md §2",
  "themes": {
    "dark": {
      "selector": "[data-theme='dark']",
      "default": true,
      "$description": "Dark theme — default for clinical use. Reduces eye strain in low-light ICU environments."
    },
    "light": {
      "selector": "[data-theme='light']",
      "$description": "Light theme — available for daytime use or user preference."
    }
  }
}
