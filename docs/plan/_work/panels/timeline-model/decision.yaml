topic: timeline-model
winner: c
totals:
  a: 3.65
  b: 4.1
  c: 4.15
vetoes: {}
must_fix:
- '''Score parcial'' semantics must be clinically ratified per score: which missing components invalidate vs degrade
  (e.g. NEWS2 without consciousness), so the caveat is principled, not cosmetic (HAZ-030).'
- A continuous 24h score trend must be reachable in one interaction from the default depth (PER-CARLOS-03, MOT-07)
  — chip-spine plus third-click per-component sparklines is too much disclosure for the 'visualização rápida' criterion.
- Chip trend arrows need an explicit delta baseline rule (window, sparse-sampling) — a wrong arrow on the spine
  is false reassurance (HAZ-031).
- Pair this surface explicitly with the home-surface dashboard in the plan so PER-FERNANDA-01 (live floor view)
  is demonstrably covered elsewhere — the scope boundary is honest but must not become a coverage gap (MOT-03).
- Point-critical labs must remain triage-visible from the spine/alert rail without expanding any table — a critical
  K+ must never require disclosure clicks to be seen (HAZ-023-class).
- 'Points-column ratification gate stays mandatory before ship: only published, versioned scorer mappings render,
  unverified mappings blocked (their R3) — an invented point table is a patient-safety and trust failure.'
- Rafael's outcome documentation must be a real structured desfecho flow (melhorou / transferido para UTI, etc.),
  not the SBAR dialog doing double duty — PER-RAFAEL-03's under-1-minute instrument measures form-open to save (MOT-16).
- The Points column renders ONLY from the ratified, versioned scorer service; UI-side point tables are forbidden,
  and any component mapping not yet verified is blocked from display until its RATIFY completes (SYS-C-03; HAZ-001/HAZ-002/HAZ-003
  P0 scoring hazards; HAZ-016 display==compute).
salvage:
- Burst-collapse ×N by dedup key and visually quiet melhora events.
- Co-occurrence-not-causation caption discipline for correlation explanations (advisory boundary, VIS-C-08).
- Correlation as a first-class stream event with 'por quê?' back-links to both source events (MOT-13).
- Dashed/hatched stale trailing segments + freeze-on-disconnect with last-sync banner — never fake continuity (MOT-04,
  MOT-18).
- Dashed/hatched trailing segment past staleness_max + freeze-on-disconnect with last-sync banner (never presents
  stale as live) — graft anywhere (HAZ-024).
- Gap events ('Sem gasometria há 6h', 'Sem RASS registrado há 8h') — absence of expected data as a surfaced clinical
  change; no other concept expresses this (MOT-04, MOT-17).
- Gap events — absence of expected data surfaced as a first-class clinical change (the strongest HAZ-030/HAZ-024
  mechanism across all nine concepts).
- Markers-only sparse labs — never interpolate data that does not exist.
- Per-event algorithm-version chip (HAZ-034 / VIS-C-13).
- Per-event provenance chips (FHIR source + algorithm version) — audit on every card (VIS-C-13).
- Prior-value + timestamp provenance on every delta headline (HAZ-031).
- Score-lane expansion into per-parameter point-contribution bands over time (PER-ANA-02 answered on the trend,
  MOT-06).
- Since-last-handoff delta digest + SBAR as a deterministic projection of the stream (PER-FERNANDA-02).
- Sparse labs as markers-only, never interpolated lines (no fabricated continuity).
- The since-last-handoff delta digest anchored to shift markers (MOT-17).
- The synchronized scrub cursor as the correlation explanation — co-occurrence as a spatial fact at one timestamp,
  no black box (MOT-13).
- The what-changed delta rail ranked by contribution to score change (MOT-01, MOT-07).
- What-changed delta rail ranked by contribution to score change.
scorers:
- clinical-safety-officer
- clinical-ux-researcher
signoffs: []
