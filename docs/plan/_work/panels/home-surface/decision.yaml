topic: home-surface
winner: c
totals:
  a: 3.7
  b: 3.525
  c: 4.0
vetoes: {}
must_fix:
- Contractually keep Spatial as default with pinning; validate Acuity-flow reordering with nurses before it can
  be a role default (their risk 1; MOT-01).
- Distinguish 'not evaluated — required data missing' from 'evaluated, no alert' on the cell (HAZ-030); the ribbon's
  acuity distribution must likewise exclude not-evaluated beds from reading as 'normal'.
- Flow-sort migration must freeze under an in-progress pointer/keyboard action so the triggering-parameter-chip
  ack can never retarget to the wrong bed (HAZ-026 masking path).
- Ribbon SLA gauge and cell states must come from the same authoritative feed with no client-side re-derivation
  (they claim ADR-C-13 — verify by contract test so grid and KPIs cannot disagree).
- Ribbon collapse for bedside roles must never remove the crítico count / cross-unit awareness — a collapsed ribbon
  still needs the acuity floor visible (MOT-03, MOT-05).
- 'Separate the read target from the act target: the triggering-parameter chip doubling as the 1-click acknowledge
  invites accidental acks that would corrupt PER-ANA-03 click-path telemetry and the VIS-7.1-03 time-to-action clock
  (MOT-08); keep 1-click but on a dedicated affordance or with an inline undo.'
- Trajectory arrows (rising/steady/falling) must have an explicit, tested baseline/delta rule — window, sparse-sampling
  behavior, tie-break — since a wrong arrow is false reassurance (HAZ-031); vectors computed on inputs past staleness_max
  must inherit the staleness mark or be suppressed (HAZ-024).
- Trajectory arrows must suppress or visibly caveat when computed on inputs past staleness_max — a rising/falling
  arrow from stale data misleads triage (MOT-04).
salvage:
- '''Meus pacientes'' dim-not-navigate filter — same surface, zero navigation for Ana''s 4-5 beds.'
- '''Only unattended criticals animate'' + ASSISTIDO desaturation: the wall shows severity no one is holding — the
  best attention-budget rule in this panel (MOT-05, MOT-18).'
- Acuity summary strip as an alarm-fatigue pressure gauge for the coordinator.
- Acuity summary strip as an always-on alarm-fatigue pressure gauge (crítico N · urgente N ...).
- Distinct vazio/reservado/higienização bed states (empty never confusable with NEUTRO occupied).
- Distinct vazio/reservado/higienização bed states so empty is never confusable with stable-occupied.
- Explicit 'Vago' dashed treatment distinct from occupied-NEUTRO.
- Full-depth breadcrumb reconstruction from a deep link as RRT location wayfinding (PER-RAFAEL-02, MOT-15).
- Full-depth breadcrumb reconstruction from a deep-linked push — turns an RRT arrival into oriented wayfinding (PER-RAFAEL-02).
- Only-unattended-criticals-animate + ASSISTIDO desaturation choreography (shows 'severity no one is holding', HAZ-023
  lever).
- Per-unit acuity badges on the context switcher (worst active severity visible before entering a unit).
- Realtime freshness pill (ao vivo / reconectando / dados defasados) as the single shared staleness truth (MOT-04).
- Realtime freshness pill + per-tile 'dados defasados' staleness veil (HAZ-024).
- 'The L1-hard rule: triggering parameter is a tile-level element, never hidden behind disclosure (PER-ANA-02, MOT-06).'
- The formalized L0-L4 progressive-disclosure ladder owned by one overlay-stack manager.
- Triggering parameter as a hard L1 tile element — never hidden behind disclosure (PER-ANA-02/PER-C-04).
- Unit-level acuity badges on the context switcher — seeing a critical on an unopened unit before switching into
  it (MOT-03).
scorers:
- clinical-safety-officer
- clinical-ux-researcher
signoffs: []
