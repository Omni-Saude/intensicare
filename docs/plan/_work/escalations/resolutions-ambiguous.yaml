scope: all 56 AMBIGUOUS items
records:
- item_id: ESC-AMBIGUOUS-293
  rule_id: RULE-ALERTAS-014
  outcome: RATIFY
  recommendation: 'The disabled ''Risco para SEPSE: realizar abertura do Protocolo Institucional'' fixed
    message on RED sepse pathways is commented-out dead code of unclear intent (retired vs. accidentally
    disabled). This gates a sepsis-protocol prompt with real clinical stakes, so a clinician/product owner
    must confirm whether v2 should re-fire it.'
- item_id: ESC-AMBIGUOUS-294
  rule_id: RULE-ALERTAS-025
  outcome: RETIRE-RECOMMENDED
  recommendation: Purely a UI color-token vocabulary (success/info/warning/danger/error) with no clinical
    logic or threshold attached. v2's design system will define its own semantic palette; nothing clinical
    to carry forward.
- item_id: ESC-AMBIGUOUS-295
  rule_id: RULE-ANTIMICROBIANO-002
  outcome: RETIRE-RECOMMENDED
  recommendation: Extraction explicitly notes this alert-color logic is legacy and unused (dead calcular_alerta
    path). No active clinical behavior to preserve.
- item_id: ESC-AMBIGUOUS-296
  rule_id: RULE-VENTILACAO-016
  outcome: RETIRE-RECOMMENDED
  recommendation: Ventilacao v1 alert coloring is unused legacy code (superseded by v3 trilha_automatica).
    No live clinical decision depends on it; safe to drop.
- item_id: ESC-AMBIGUOUS-297
  rule_id: RULE-BALANCO-HIDRICO-045
  outcome: RATIFY
  recommendation: Encodes the recurring 'sign to finalize, soft-delete not hard-delete' medical-legal
    recordkeeping pattern for fluid-balance entries (ativo/data_assinatura/deletado_por). This audit-trail
    semantics for clinical records needs explicit product ratification for v2's documentation lifecycle.
- item_id: ESC-AMBIGUOUS-298
  rule_id: RULE-EVOLUCOES-011
  outcome: RATIFY
  recommendation: can_liberar gates release of a clinical evolution note on tipo+liberar+cpf+nr_atendimento
    simultaneously — a medical-legal sign-off gate on clinical documentation. Whether all four conditions
    are truly required (vs. dead/unused code) needs product confirmation before v2 encodes it.
- item_id: ESC-AMBIGUOUS-299
  rule_id: RULE-EVOLUCOES-017
  outcome: RATIFY
  recommendation: Physician-form vitals submission auto-creates/attaches a daily balanco_hidrico record
    via a possibly-broken .get_pk call; a failure here silently blocks vital-signs capture (feeds SOFA/qSOFA/MEWS).
    The vitals-to-fluid-balance coupling and its failure mode need clinical/product review, not silent
    redesign.
- item_id: ESC-AMBIGUOUS-300
  rule_id: RULE-FORMULARIOS-CLINICOS-017
  outcome: RETIRE-RECOMMENDED
  recommendation: A cosmetic icon assignment for the 'intercorrencia' concept in the UI; carries no clinical
    threshold or decision logic. v2's icon set is a design decision, not a clinical rule.
- item_id: ESC-AMBIGUOUS-301
  rule_id: RULE-FORMULARIOS-CLINICOS-037
  outcome: RETIRE-RECOMMENDED
  recommendation: Discipline-to-icon mapping for the multidisciplinary care team is pure UI iconography
    with no clinical semantics. Superseded by v2 design system.
- item_id: ESC-AMBIGUOUS-302
  rule_id: RULE-PRESCRICAO-028
  outcome: RATIFY
  recommendation: Medication administration/cancellation lifecycle (administrado/suspenso/motivo_nao_administrado/justificativa_cancelamento)
    is patient-safety relevant; extraction flags that a non-administered dose can be submitted without
    a reason. Whether motivo_nao_administrado should be hard-required needs clinical/compliance sign-off.
- item_id: ESC-AMBIGUOUS-303
  rule_id: RULE-PROFILAXIA-008
  outcome: RATIFY
  recommendation: The active v3 prophylaxis facade fires on only 2 of the original 9 criteria (LAMGD +
    insertion bundle); criteria 3-8 are dead copy-pasted sepsis thresholds, not real prophylaxis logic.
    Whether this narrowing to 2 criteria is intentional or an incomplete migration is a clinical-scope
    decision v2 must not silently inherit or silently expand.
- item_id: ESC-AMBIGUOUS-304
  rule_id: RULE-SEDACAO-022
  outcome: RATIFY
  recommendation: Defines 'cardiac arrest within last 24h' as timedelta.days==0 (a rolling <24h window,
    not a calendar day), feeding manual sedation criteria C4/C6. This temporal-window semantics directly
    affects sedation-protocol eligibility and needs explicit clinical confirmation before v2 encodes it.
- item_id: ESC-AMBIGUOUS-305
  rule_id: RULE-SEPSE-086
  outcome: RETIRE-RECOMMENDED
  recommendation: 'A frontend history-tab query filter ({aceito: false}) whose semantics (''pending review''
    vs. ''rejected'') is an unconfirmed inference from a single partition with no supporting logic. Unusable
    as extracted; v2 should design pathway-history state explicitly rather than port an ambiguous filter.'
- item_id: ESC-AMBIGUOUS-306
  rule_id: RULE-SEPSE-093
  outcome: RATIFY
  recommendation: 'Two distinct sepsis-pathway completion flags (finalizado vs. concluida) with no code
    distinguishing them in this partition (confidence: low). Sepsis-bundle completion tracking is a core
    quality/compliance metric, so the true state machine needs clinical/backend confirmation, not a guessed
    re-implementation.'
- item_id: ESC-AMBIGUOUS-307
  rule_id: RULE-TRILHAS-ENGINE-003
  outcome: RETIRE-RECOMMENDED
  recommendation: get_trilha's 'automatica' branch picks a pathway record with no explicit ordering (implementation-dependent).
    This is a data-access implementation detail that v2's proper encounter-scoped data model will resolve
    by design; no clinical judgment call is needed, just correct schema/query design.
- item_id: ESC-AMBIGUOUS-308
  rule_id: RULE-TRILHAS-ENGINE-012
  outcome: RATIFY
  recommendation: v3 pathway bootstrap re-fetches pathway instances via .first() with no nr_atendimento
    filter, risking re-pointing a stale prior-encounter pathway record's leito to a new admission. Whether
    pathways are lifetime-per-patient or per-encounter is a clinical data-model decision that must be
    confirmed before v2, not silently assumed.
- item_id: ESC-AMBIGUOUS-309
  rule_id: RULE-TRILHAS-ENGINE-013
  outcome: RETIRE-RECOMMENDED
  recommendation: A cosmetic display-name humanizer (hardcoded 6-char 'trilha' prefix split). Pure UI
    string formatting with no clinical content; v2 can name pathways directly.
- item_id: ESC-AMBIGUOUS-310
  rule_id: RULE-TRILHAS-ENGINE-015
  outcome: RATIFY
  recommendation: Requiring a mandatory free-text justification (motivo_descartado) to refuse a care-pathway
    protocol is a medical-legal documentation safeguard preventing silent protocol dismissal. This requirement
    should be explicitly carried into v2's pathway-refusal workflow, pending product confirmation.
- item_id: ESC-AMBIGUOUS-311
  rule_id: RULE-DOCUMENTACAO-FATURAMENTO-014
  outcome: RETIRE-RECOMMENDED
  recommendation: A double permission-gate (SSR cookie check + client hook re-check) on the evolution
    report page is an access-control implementation detail, not clinical logic. v2's RBAC will implement
    a single authoritative permission check by design.
- item_id: ESC-AMBIGUOUS-312
  rule_id: RULE-SEPSE-066
  outcome: RATIFY
  recommendation: Documents 7 sepsis criteria (invasive-device duration, recent abdominal surgery, early
    antibiotics, early noradrenaline, urea+creatinine, leukocyte abnormality/delta) silently dropped from
    a 27-criterion legacy set to the current 20. These are clinically substantive sepsis risk signals
    whose omission from v2 must be an explicit clinical decision, not a silent extraction gap.
- item_id: ESC-AMBIGUOUS-313
  rule_id: RULE-AUDITORIA-LOGS-025
  outcome: RETIRE-RECOMMENDED
  recommendation: A UUID-in-path display normalization not applied to the underlying route-aggregation
    stats is an audit-dashboard analytics quirk, not a clinical rule. v2's telemetry pipeline should normalize
    consistently by design.
- item_id: ESC-AMBIGUOUS-314
  rule_id: RULE-AUDITORIA-LOGS-028
  outcome: RETIRE-RECOMMENDED
  recommendation: Authenticated-user attribution on audit log entries is generic infrastructure logging
    behavior with no clinical decision content. Not a candidate for the clinical rule catalog.
- item_id: ESC-AMBIGUOUS-315
  rule_id: RULE-AUDITORIA-LOGS-032
  outcome: RETIRE-RECOMMENDED
  recommendation: An is_html content-sniffing heuristic with broad false-positive risk is a technical
    logging/rendering concern, unrelated to clinical care logic. Safe to drop and reimplement cleanly
    if needed.
- item_id: ESC-AMBIGUOUS-316
  rule_id: RULE-AUDITORIA-LOGS-034
  outcome: RETIRE-RECOMMENDED
  recommendation: A country->region->city cascading filter bug (city not re-scoped by country) in an admin
    log-filtering UI. Pure UI filter-state bug with no clinical bearing.
- item_id: ESC-AMBIGUOUS-317
  rule_id: RULE-AUTH-USUARIOS-056
  outcome: RETIRE-RECOMMENDED
  recommendation: exceto_metodo literally misspells PATCH as PATH across write viewsets — an obvious legacy
    typo/bug, not an intentional rule. Nothing clinical to preserve; v2's permission checks should just
    be written correctly.
- item_id: ESC-AMBIGUOUS-318
  rule_id: RULE-BALANCO-HIDRICO-062
  outcome: RETIRE-RECOMMENDED
  recommendation: Extraction notes this day-scoped fluid-balance filter is unused. No live behavior depends
    on it; legacy dead code.
- item_id: ESC-AMBIGUOUS-319
  rule_id: RULE-CADASTROS-UI-001
  outcome: RETIRE-RECOMMENDED
  recommendation: FilterLeitos sends a tri-state occupancy filter as a literal string rather than a structured
    value — a frontend serialization bug in an admin bed-listing UI, not a clinical rule.
- item_id: ESC-AMBIGUOUS-320
  rule_id: RULE-CADASTROS-UI-005
  outcome: RETIRE-RECOMMENDED
  recommendation: New-user password auto-filled from the user's own CPF is a security anti-pattern (predictable
    credential), not clinical logic. Flag separately for security remediation; v2's user-provisioning
    should generate secure credentials by design rather than port this behavior.
- item_id: ESC-AMBIGUOUS-321
  rule_id: RULE-CADASTROS-UI-006
  outcome: RATIFY
  recommendation: Every user's electronic-signature PIN defaults to the same hardcoded literal ('Homecare@Vidaconecta'),
    undermining the non-repudiation of clinical sign-offs (prescriptions, evolutions, sepsis checklists)
    that the platform otherwise treats as medical-legally significant. Whether real per-user PINs exist
    server-side must be confirmed before v2 designs its signature-integrity model.
- item_id: ESC-AMBIGUOUS-322
  rule_id: RULE-COMUNICACAO-041
  outcome: RETIRE-RECOMMENDED
  recommendation: Observation setor_id is always forced from the URL rather than from payload — a request-binding
    implementation detail in a messaging feature, not a clinical threshold or decision.
- item_id: ESC-AMBIGUOUS-323
  rule_id: RULE-COMUNICACAO-045
  outcome: RETIRE-RECOMMENDED
  recommendation: FilePicker only reconciles its local file list on removal (a UI state-sync bug in file
    uploads), unrelated to clinical logic.
- item_id: ESC-AMBIGUOUS-324
  rule_id: RULE-DOCUMENTACAO-FATURAMENTO-017
  outcome: RETIRE-RECOMMENDED
  recommendation: Default sort order for a bed-file listing UI is a cosmetic presentation choice, not
    a clinical rule.
- item_id: ESC-AMBIGUOUS-325
  rule_id: RULE-DOCUMENTACAO-FATURAMENTO-024
  outcome: RATIFY
  recommendation: Cross-references a Trilhas-vs-CPOE prescription-item signature-inconsistency record
    used for medication-order reconciliation; the actual mismatch-detection algorithm lives server-side
    and is not captured. This medication-safety reconciliation concept needs clinical/compliance confirmation
    on its detection logic before v2 rebuilds it.
- item_id: ESC-AMBIGUOUS-326
  rule_id: RULE-EVOLUCOES-018
  outcome: RETIRE-RECOMMENDED
  recommendation: A company-wide evolutions queryset filter with a possible instance-vs-id type mismatch
    is a query-construction bug, not a clinical decision rule.
- item_id: ESC-AMBIGUOUS-327
  rule_id: RULE-EVOLUCOES-022
  outcome: RETIRE-RECOMMENDED
  recommendation: A single-checkbox form 'conditions' key mismatch is a frontend form-wiring bug with
    no clinical semantics attached.
- item_id: ESC-AMBIGUOUS-328
  rule_id: RULE-EVOLUCOES-048
  outcome: RETIRE-RECOMMENDED
  recommendation: Dual query-parameter encoding on an evolution-report fetch endpoint is an HTTP/request-plumbing
    quirk, not a clinical rule.
- item_id: ESC-AMBIGUOUS-329
  rule_id: RULE-EVOLUCOES-074
  outcome: RATIFY
  recommendation: Form-group 'annulment' (soft-void rather than delete, via anulavel/isAnnulled) is inferred
    to preserve a medical-legal audit trail for clinical-record groups, matching the recurring sign/void-not-delete
    theme elsewhere in the system. Confidence is low (shape-only inference, no driving code found); product
    must confirm the intended semantics before v2 builds a void mechanic.
- item_id: ESC-AMBIGUOUS-330
  rule_id: RULE-FORMULARIOS-CLINICOS-039
  outcome: RETIRE-RECOMMENDED
  recommendation: Binary Masculino/Feminino iconography is a cosmetic UI choice, not a clinical rule or
    threshold.
- item_id: ESC-AMBIGUOUS-331
  rule_id: RULE-MOVIMENTACAO-ADT-048
  outcome: RETIRE-RECOMMENDED
  recommendation: A fixed 3-second minimum loading-spinner on the bed camera page is a cosmetic UX timing
    choice with zero clinical content.
- item_id: ESC-AMBIGUOUS-332
  rule_id: RULE-MOVIMENTACAO-ADT-049
  outcome: RETIRE-RECOMMENDED
  recommendation: Test/temporary-bed exclusion uses AND semantics (all three substrings must match) where
    the naming implies OR was intended, letting many test beds leak into ETL. This is a data-hygiene bug
    in bed-data ingestion, not a clinical rule; v2 should just tag test beds explicitly rather than sniff
    for them.
- item_id: ESC-AMBIGUOUS-333
  rule_id: RULE-MOVIMENTACAO-ADT-058
  outcome: RETIRE-RECOMMENDED
  recommendation: Extraction itself flags this as unverifiable — the observed 400 is fully explained by
    a confounding empty-data_entrada rule, so any 'automatica'-specific requirement cannot be confirmed
    from tests alone. Legacy validation quirk, not a distinct clinical rule.
- item_id: ESC-AMBIGUOUS-334
  rule_id: RULE-OPERACIONAL-INFRA-017
  outcome: RETIRE-RECOMMENDED
  recommendation: ParanoiaMixin's admin-path hard-delete vs. cascading soft-delete split is a Django ORM/infra
    data-lifecycle mechanic, not clinical logic. v2's persistence layer will define its own delete semantics.
- item_id: ESC-AMBIGUOUS-335
  rule_id: RULE-OPERACIONAL-INFRA-037
  outcome: RETIRE-RECOMMENDED
  recommendation: ListChoiceField's context-dependent serialization switch is a DRF serializer implementation
    detail, unrelated to any clinical threshold or decision.
- item_id: ESC-AMBIGUOUS-336
  rule_id: RULE-PRESCRICAO-036
  outcome: RATIFY
  recommendation: A validator preventing any alteration to a dose's 'administrado' status once set is
    unused in this partition, but per the extractor's own note it 'encodes a real business rule even if
    apparently dead code' — locking medication-administration records against tampering is patient-safety
    and medical-legal relevant. Whether it should be wired in for v2 needs clinical/compliance confirmation.
- item_id: ESC-AMBIGUOUS-337
  rule_id: RULE-SEPSE-068
  outcome: RATIFY
  recommendation: The urea input field (key 'ureia_maior_50_24hrs') is stored as an unbounded raw number
    despite its name implying a >50 mg/dL-in-24h threshold flag used elsewhere in sepsis criteria (RULE-SEPSE-066/067).
    Whether this field is a raw lab value or a boolean threshold materially changes sepsis-criterion interpretation
    and needs clinical clarification.
- item_id: ESC-AMBIGUOUS-338
  rule_id: RULE-SEPSE-098
  outcome: RATIFY
  recommendation: Sepsis-checklist signers uniquely capture a CPF (unlike Prescricao/BalancoHidrico/Chat
    'checked-by' actors), suggesting a stronger medical-legal accountability requirement for sepsis-bundle
    compliance given its severity/liability profile. Whether this stricter signature requirement should
    be formalized platform-wide or is sepsis-specific needs product/clinical confirmation.
- item_id: ESC-AMBIGUOUS-339
  rule_id: RULE-SINAIS-VITAIS-033
  outcome: RATIFY
  recommendation: SpO2 validation (21-100, no zero exemption) is defined but its enforcement is commented
    out on the model field, so SpO2 is effectively unvalidated even though it directly feeds sepsis (<96)
    and ventilation (>96/>92) criteria. Unvalidated vital-sign input feeding clinical scoring is a patient-safety
    data-quality gap that must be resolved with an explicit validation range for v2, not silently re-disabled.
- item_id: ESC-AMBIGUOUS-340
  rule_id: RULE-TENANCY-ORGANIZACAO-020
  outcome: RETIRE-RECOMMENDED
  recommendation: LeitoMiddleware's missing cross-tenant consistency check (unlike the sibling Empresa/Estabelecimento/Setor
    middlewares) is a multi-tenant security-architecture gap, not a clinical rule. v2's tenancy layer
    must implement cross-tenant checks by design for every resource; this is a fix to make, not a clinical
    judgment call to ratify.
- item_id: ESC-AMBIGUOUS-341
  rule_id: RULE-OPERACIONAL-INFRA-040
  outcome: RETIRE-RECOMMENDED
  recommendation: Per-environment deploy scripts with asymmetric env-file loading are pure DevOps/CI plumbing
    (PM2/legacy-app deployment mechanics), explicitly out of scope for the platform-change rebuild.
- item_id: ESC-AMBIGUOUS-342
  rule_id: RULE-OPERACIONAL-INFRA-059
  outcome: RETIRE-RECOMMENDED
  recommendation: A per-company dashboard auto-refresh interval is an operational UI configuration knob,
    not a clinical threshold or decision rule.
- item_id: ESC-AMBIGUOUS-343
  rule_id: RULE-AUDITORIA-LOGS-021
  outcome: RETIRE-RECOMMENDED
  recommendation: A soft-delete mixin present on the log model but never exercised is unused legacy infrastructure
    scaffolding, not a clinical rule.
- item_id: ESC-AMBIGUOUS-344
  rule_id: RULE-AUDITORIA-LOGS-022
  outcome: RETIRE-RECOMMENDED
  recommendation: Log dashboard access control (authenticated-only, no staff/ownership check) is an access-control
    gap in an internal admin tool, not a clinical decision. v2's RBAC should scope this properly by design.
- item_id: ESC-AMBIGUOUS-345
  rule_id: RULE-AUTH-USUARIOS-045
  outcome: RETIRE-RECOMMENDED
  recommendation: A backend-vs-frontend type mismatch on user access-role codes (proprietario/usuario/monitor)
    is an RBAC naming/typing inconsistency, not clinical logic. v2's access model will define roles fresh.
- item_id: ESC-AMBIGUOUS-346
  rule_id: RULE-AUTH-USUARIOS-047
  outcome: RETIRE-RECOMMENDED
  recommendation: A dormant read/read-write access-level enumeration with no active enforcement path is
    unused RBAC scaffolding, not a clinical rule.
- item_id: ESC-AMBIGUOUS-347
  rule_id: RULE-DOCUMENTACAO-FATURAMENTO-015
  outcome: RETIRE-RECOMMENDED
  recommendation: Auto-posting a released evolution to the Tasy EMR (billing lancamento code 501) is exactly
    the Tasy-direct-ETL/legacy-integration plumbing the platform change retires (CONTRACTS.md RETIRE example).
    No clinical logic to carry forward — only integration mechanics tied to a system being replaced.
- item_id: ESC-AMBIGUOUS-348
  rule_id: RULE-FORMULARIOS-CLINICOS-040
  outcome: RETIRE-RECOMMENDED
  recommendation: Duplicate route registration for the enfermagem (nursing) form is a routing-configuration
    duplication bug, not a clinical rule.
