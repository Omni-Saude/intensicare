# FIX_HANDOFF.yaml — Estado dos sprints de fechamento de gaps (pós-auditoria NO-GO 63.8)
meta:
  plan: "/Users/familia/.claude/plans/1-build-a-detailed-bubbly-waffle.md (aprovado 2026-07-12)"
  orchestrator: "Claude (Fable 5) — nunca coda; delega, verifica, adjudica"
  protocol: "branch por sprint → swarm (≤3 arquivos/agente) → verificação real pós-agente → gate por revisor independente → PR + CI → merge"

sprints:
  sprint_1_patient_safety:
    branch: fix/sprint-1-patient-safety
    status: in_progress
    items:
      S1.0_async_bug: done_verified       # evaluate async; fail-fast provado; validate_alerts compila predicates
      S1.0b_criteria_value_500: done_verified  # coerção str preservando contrato TS; 200 nos 4 endpoints
      S1.0c_test_vitals_mews_assert: done_verified  # regressão detectada por diff main-vs-branch; conta documentada
      logo_brand: done_verified            # asset SVG + AppShell + login + favicon; aprovação visual do orquestrador
      S1.1_migrations: done_verified      # 0037+0038; idempotência provada em DB descartável; chain 0036→0039
      S1.2_dao_service: done_verified     # 1103 linhas, mypy clean; guard duplo anti-race; ws publish lazy
      S1.3_boot_sync: done_verified       # 12 pathways no PG com hash real; idempotente; app.state engine
      S1.4_router: done_verified          # 6 endpoints com get_db; 201/409/200 provados; C-13 fechado
      S1.5_mews: done_verified            # v3.0.0; 38.5→2, 40→1 provados; 76/76 testes; migração 0039
      S1.6_yaml_bands: done_verified      # 3 critérios ressuscitados; engine compila 12/12 sem warnings
      S1.7_fail_fast: done_verified
      S1.8_rbac: done_verified            # bypass removido; 79/79 security tests; aliases PT→EN
      S1.8b_admin_backfill: done_verified # migração 0040 + UserCreate.role validado
      S1.9_seed: done_verified            # 5 DEMO; 2× idempotente; dashboard vivo confirma critical
      S1.10_wave3_tests: done_verified    # 31+8 testes novos verdes; restart-survival + sentinela anti-legacy; conftest MEWS-v3
    discovered_issues:
      - "alembic: revision 0002 duplicada (heads desconexos) + 0016 trunca SODIUM_CORRECTION em VARCHAR(16) — upgrade from-scratch quebra ANTES de 0037; pré-existente, anotar para gate/G-S1"
      - "UserCreate não expunha role — corrigido em S1.8b"
    gate: G-S1 pending
  sprint_2_integration: {status: pending}
  sprint_3_sepsis_governance: {status: pending}
  sprint_4_reaudit: {status: pending}
